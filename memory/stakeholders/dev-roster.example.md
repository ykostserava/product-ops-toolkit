# Dev roster (example - copy to dev-roster.md and fill in)

Read by the `project-manager` agent (R4 candidate assignees) per
`patterns/jira-read-protocol.md`. Keep usernames exact; aliases are for
matching what people are called in chat and comments.

| Name / aliases | Jira username | Components | Role | Unavailable until |
|---|---|---|---|---|
| Alex Example (Alex, AE) | alex.example@example.com | Backend | dev | |
| Sam Example (Sam) | sam.example@example.com | iOS, Android | dev | 2026-01-15 |
| Robin Example (Robin) | robin.example@example.com | Web | dev | |
| Kim Example (Kim) | kim.example@example.com | QA | qa | |

Semantics: `role: qa` = candidate only for testing work; a PAST
"Unavailable until" date means the person is back (stale entries never hide
anybody); a name matching more than one row is ambiguous - ask, never guess.
